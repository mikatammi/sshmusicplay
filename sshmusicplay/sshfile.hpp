#ifndef SSHFILE_HPP
#define SSHFILE_HPP

#include <QObject>

class SSHFile : public QObject
{
    Q_OBJECT
public:
    explicit SSHFile(QObject *parent = 0);
    
signals:
    
public slots:
    
};

#endif // SSHFILE_HPP
